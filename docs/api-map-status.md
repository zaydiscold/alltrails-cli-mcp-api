# API Map Status

Checked: 2026-05-26

Evidence so far:

- A preflight search found no existing AllTrails API map or CLI to build on.
- Community projects confirm `/api/alltrails/v3/trails/{trail_id}?detail=offline` and public trail page embedded/static-map surfaces.
- A logged browser tab on port `9227` showed public trail page resources, DataDome presence, route families for trails, parks, POIs, member pages, recordings, Plus, and static-map image proxy calls.
- Authenticated account endpoints remain pending until a capture has clear account/session evidence.

